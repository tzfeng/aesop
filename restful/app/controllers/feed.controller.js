const Bet = require('../models/bet.model.js');
const moment = require('moment');

exports.feed = async function(req, res) {
  // get all bets that are active

  let betIDs = []

  Bet.find()

  for (let bet in bets) {

  }

  
  query.exec(function (err, docs) {});

  // do smth
}